{
  "name": "learnflow-ai",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "uvicorn app.main:app --host 0.0.0.0 --port 3000 --reload",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 3000",
    "build": "python3 -c 'import app.main, app.domain, app.repositories, app.storage; print(\"Build successful\")'",
    "lint": "python3 -m pytest -q tests/"
  }
}
